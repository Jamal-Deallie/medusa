import { Card } from '../../components';
import { ProductWrapper } from './styles';

export default function ProductsListContainer({ items }) {
  return (
    <ProductWrapper>
     <Card/>
    </ProductWrapper>
  );
}
